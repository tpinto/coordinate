<div class="comment clearfix">
	<p class="body"><%= h comment.body %></p>
	<span class="commenter"><a class="avatar" href="<%=user_path(comment.user)%>"><%= image_tag comment.user.gravatar(16), :alt => "" %> <%= comment.user %></a>, <%=time_ago_in_words(comment.created_at)%> ago:</span>
</div>